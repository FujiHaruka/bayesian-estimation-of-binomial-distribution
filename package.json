{
  "name": "bayesian-estimation-of-binomial-distribution",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "parcel serve src/index.html --dist-dir dist-dev",
    "build": "parcel build src/index.html --dist-dir dist --public-url /bayesian-estimation-of-binomial-distribution/",
    "format": "prettier --write src/",
    "typecheck": "tsc --noEmit",
    "deploy": "git clean -fdx dist/ && npm run build && gh-pages -d dist/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "FujiHaruka",
  "license": "MIT",
  "dependencies": {
    "@picocss/pico": "^1.5.7",
    "chart.js": "^4.2.1",
    "gh-pages": "^5.0.0",
    "jstat": "^1.9.6",
    "mathjax": "^3.2.2",
    "parcel": "^2.8.3",
    "prettier": "^2.8.4",
    "typescript": "^4.9.5"
  }
}
